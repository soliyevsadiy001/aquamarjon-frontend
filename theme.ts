// ============================================================
// Единая палитра приложения.
// Раньше hex-цвета (#00C9B1, #1C3A4A, #102433 и т.п.) были вписаны
// литералами в 1600+ мест по всему App.tsx — смена брендового цвета
// требовала find&replace по всему файлу с риском задеть чужой
// оттенок с похожим кодом. Теперь единый источник правды: меняем
// значение здесь — перекрашивается всё приложение.
//
// Существовавший ранее объект THEME (тёмная тема seller/courier
// кабинетов) сохранён как есть и расширен остальными часто
// встречающимися оттенками — так старые обращения `THEME.bg` и т.д.
// продолжают работать без правок в местах использования.
// ============================================================

export const COLORS = {
  // База (было THEME)
  bg: "#08131F",
  card: "#0E2030",
  border: "#1C3A4A",
  teal: "#00C9B1",
  amber: "#F0A93C",
  text: "#E8F4F8",
  muted: "#6C8E96",
  soft: "#9FC4CC",
  red: "#FF6B6B",
  green: "#51CF66",

  // Часто встречающиеся дополнительные оттенки (топ по частоте в файле)
  panel: "#102433",     // вторичная тёмная панель/подложка карточек
  bg2: "#0B1B28",        // альтернативный фон секций
  tealDark: "#00A896",   // тёмный тил (концы градиентов, hover)
  greenBg: "#0F2A26",    // тёмно-зелёная подложка (успех/эко-бейджи)
  greenBg2: "#071C14",   // ещё темнее зелёный (карточки достижений)
  green2: "#4ADE80",     // альтернативный ярко-зелёный акцент
  redBg: "#2A1414",      // тёмно-красная подложка (ошибки/предупреждения)
  soft2: "#C9DEE2",      // светлее soft, для второстепенного текста на карточках
} as const;

/** Готовые повторяющиеся градиенты — раньше одна и та же строка
 * градиента была продублирована текстом в нескольких компонентах
 * (IconBadge, бейджи, кнопки). */
export const GRADIENTS = {
  tealPrimary: `linear-gradient(135deg, ${COLORS.teal}, ${COLORS.tealDark})`,
  amber: `linear-gradient(135deg, ${COLORS.amber}, #C97F1F)`,
  redPink: "linear-gradient(135deg, #FF5C7A, #E23F5D)",
  blue: "linear-gradient(135deg, #2E86FF, #1D5FCC)",
} as const;

/** @deprecated используйте COLORS — оставлено для обратной совместимости
 * со старыми обращениями `THEME.xxx` внутри App.tsx. */
export const THEME = COLORS;
